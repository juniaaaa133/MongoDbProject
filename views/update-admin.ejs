<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/index.css">
    <title><%= post.title %></title>
</head>
<body>
    <h1>Update Item : <%= post.title %></h1>
    <form method="post" action="/admin/item-detail/update/<%= post.id%>" class="form-ctn">
        <input type="text" name="postId" hidden value="<%= post.id %>">
        <input type="text" name="userId" hidden value="<%= post.userId %>">

        <div class="flex flex-col">
            <label for="title">Edit title</label>
            <input value="<%= post.title%>" type="text" id="title" name="title" class="inp">
        </div>
        <div class="flex flex-col">
            <label for="description">Edit description</label>
            <input value="<%= post.description %>" type="text" id="description" name="description" class="inp">
        </div>
        <div class="flex flex-col">
            <label for="image">Edit Image Url</label>
            <input value="<%= post.image %>" type="text" id="image" name="image" class="inp">
        </div>
        <button class="btn">Publish Now</button>
    </form>
</body>
</html>